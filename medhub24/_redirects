# Cloudflare Pages — redirects
# https://developers.cloudflare.com/pages/configuration/redirects/

# Internal documentation is not in this directory at all — it lives in
# docs/ at the repository root, outside the published root. Nothing to
# exclude here.

# The site is a single-document SPA; routing happens in js/app.js and
# never changes the URL. Anything else resolves to the document.
/*    /index.html   200
