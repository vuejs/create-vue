# Cloudflare Pages — redirects
# https://developers.cloudflare.com/pages/configuration/redirects/

# The site is a single-document SPA; routing happens in js/app.js and
# never changes the URL. Anything else resolves to the document.
/*    /index.html   200
