{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "npm-run-all2": "^8.0.1",
    "typescript": "~5.8.0",
    "vue-tsc": "^2.2.10"
  }
}
