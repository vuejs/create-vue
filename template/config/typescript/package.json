{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build --force"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "npm-run-all2": "^6.1.2",
    "typescript": "~5.3.0",
    "vue-tsc": "^1.8.27"
  }
}
