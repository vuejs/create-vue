{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^22.18.11",
    "npm-run-all2": "^8.0.4",
    "typescript": "~5.9.0",
    "vue-tsc": "^3.1.1"
  }
}
