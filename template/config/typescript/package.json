{
  "scripts": {
    "build": "run-p type-check \"build-only -- {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "npm-run-all2": "^8.0.4",
    "typescript": "~6.0.0",
    "vue-tsc": "^3.2.6"
  }
}
