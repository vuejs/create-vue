{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build --force"
  },
  "devDependencies": {
    "@types/node": "^20.12.11",
    "npm-run-all2": "^6.1.2",
    "typescript": "~5.4.0",
    "vue-tsc": "^2.0.17"
  }
}
