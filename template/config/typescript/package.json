{
  "scripts": {
    "build": "run-p type-check build-only",
    "build-only": "vite build",
    "type-check": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^18.14.2",
    "npm-run-all": "^4.1.5",
    "typescript": "~5.0.2",
    "vue-tsc": "^1.2.0"
  }
}
