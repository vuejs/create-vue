{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "npm-run-all2": "^7.0.2",
    "typescript": "~5.6.3",
    "vue-tsc": "^2.1.10"
  }
}
