{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^22.15.31",
    "npm-run-all2": "^8.0.4",
    "typescript": "~5.8.0",
    "vue-tsc": "^2.2.10"
  }
}
