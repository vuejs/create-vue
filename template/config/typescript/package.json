{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build --force"
  },
  "devDependencies": {
    "@types/node": "^20.16.14",
    "npm-run-all2": "^6.2.6",
    "typescript": "~5.6.0",
    "vue-tsc": "^2.1.6"
  }
}
