{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "npm-run-all2": "^9.0.2",
    "typescript": "~7.0.0",
    "vue-tsc": "^3.3.7"
  }
}
