{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "npm-run-all2": "^7.0.2",
    "typescript": "~5.8.0",
    "vue-tsc": "^2.2.8"
  }
}
