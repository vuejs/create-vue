{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build --force"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "npm-run-all2": "^6.2.2",
    "typescript": "~5.5.0",
    "vue-tsc": "^2.0.26"
  }
}
