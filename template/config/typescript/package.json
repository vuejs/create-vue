{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build --force"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "npm-run-all2": "^7.0.1",
    "typescript": "~5.6.0",
    "vue-tsc": "^2.1.6"
  }
}
