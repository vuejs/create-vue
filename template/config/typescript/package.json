{
  "scripts": {
    "build": "run-p type-check \"build-only {@}\" --",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "devDependencies": {
    "@types/node": "^24.10.7",
    "npm-run-all2": "^8.0.4",
    "typescript": "~5.9.3",
    "vue-tsc": "^3.2.2"
  }
}
